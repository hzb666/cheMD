{
  "name": "@chemd/step-ontology",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@chemd/core": "workspace:*",
    "@chemd/diagnostics": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
