{
  "name": "@chemd/importer-prose",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@chemd/chemical-lexicon": "workspace:*",
    "@chemd/compiler": "workspace:*",
    "@chemd/core": "workspace:*",
    "@chemd/step-ontology": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
