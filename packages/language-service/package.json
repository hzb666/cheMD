{
  "name": "@chemd/language-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@chemd/compiler": "workspace:*",
    "@chemd/core": "workspace:*",
    "@chemd/step-ontology": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../compiler/vitest.config.ts --pool=threads",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
