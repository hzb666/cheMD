{
  "name": "@chemd/renderer-html",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@chemd/core": "workspace:*",
    "@chemd/render-profile": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
