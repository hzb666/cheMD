# V0.1 Alignment Fixes Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Align the current codebase with the latest v0.1 product boundary and fix the blocking correctness issues found in review.

**Architecture:** Keep v0.1 scoped to `Editor + Preview`, make molecule flows safe and deterministic, add the missing `reaction.conditions` contract across the core pipeline, and correct service/doc behavior that currently misrepresents implementation status. Preserve experimental code where possible, but move it off the default path.

**Tech Stack:** TypeScript, React 19, Next.js 15 route handlers, Vitest, Python unittest, Flask

---

## Chunk 1: Lock Regressions With Tests

### Task 1: Tree Boundary Regression

**Files:**
- Modify: `apps/web/tests/page.test.tsx`

- [ ] Add a failing assertion that the default page renders `Editor` and `Preview` without requiring `Tree`.
- [ ] Run: `pnpm --filter @chemd/web test -- page.test.tsx`
- [ ] Confirm the current page test fails for the right reason.

### Task 2: `reaction.conditions` Contract Regression

**Files:**
- Modify: `packages/parser/tests/parser.test.ts`
- Modify: `packages/compiler/tests/compiler.test.ts`
- Modify: `packages/renderer-html/tests/renderer-html.test.ts`

- [ ] Add a parser test proving `reaction.conditions` is parsed as a `string[]`.
- [ ] Add compile/render tests proving conditions survive into compiled output.
- [ ] Run targeted parser/compiler/renderer tests and confirm they fail before implementation.

### Task 3: Safe Writeback Regression

**Files:**
- Modify: `apps/web/tests/ocr-molecule-writeback.test.ts`
- Modify: `apps/web/tests/load-structure-draft.test.ts`
- Create: `apps/web/tests/structure-save.test.ts`

- [ ] Add failing tests for deterministic OCR targeting in multi-molecule documents.
- [ ] Add failing tests for preserving `molfile` in save requests and saved drafts.
- [ ] Run targeted web tests and confirm the failures are due to current behavior.

### Task 4: `chem-service` Input Validation Regression

**Files:**
- Modify: `services/chem-service/tests/test_app.py`

- [ ] Add failing tests proving blank `smiles`/`molfile` are rejected by `/normalize` and `/render`.
- [ ] Run: `python -m unittest discover -s services/chem-service/tests -p 'test_*.py'`
- [ ] Confirm the failures are behavior-related, not harness-related.

## Chunk 2: Core Contract And Service Fixes

### Task 5: Add `reaction.conditions` To Core Types And Parser

**Files:**
- Modify: `packages/core/src/ast.ts`
- Modify: `packages/parser/src/body/parse-body.ts`

- [ ] Add `conditions?: string[]` to `ReactionNode`.
- [ ] Accept `conditions` as a reaction list field parsed with `|`.
- [ ] Re-run parser tests until green.

### Task 6: Carry `reaction.conditions` Through Render/Export

**Files:**
- Modify: `packages/renderer-html/src/index.ts`
- Modify: `packages/renderer-svg/src/index.ts`
- Modify: `packages/exporter-training/src/semantic-layer.ts`
- Modify: `packages/exporter-training/src/types.ts`
- Modify: `packages/exporter-training/tests/exporter-record.test.ts`

- [ ] Render conditions explicitly instead of deriving them from unrelated reaction fields.
- [ ] Preserve normalized conditions in exporter output without regressing existing behavior.
- [ ] Re-run targeted package tests until green.

### Task 7: Tighten `chem-service` Validation

**Files:**
- Modify: `services/chem-service/app.py`
- Modify: `services/chem-service/README.md`

- [ ] Reject blank chemistry inputs instead of falling back to placeholder values.
- [ ] Keep placeholder OCR explicit as non-persistable failure.
- [ ] Re-run Python tests until green.

## Chunk 3: Web Boundary And Writeback Fixes

### Task 8: Remove Tree From Default V0.1 Surface

**Files:**
- Modify: `apps/web/src/app/page.tsx`
- Modify: `apps/web/tests/page.test.tsx`

- [ ] Remove the `DocumentTreePanel` mount from the default page.
- [ ] Keep experimental Tree code and its dedicated tests intact.
- [ ] Re-run page tests until green.

### Task 9: Make OCR Targeting Deterministic

**Files:**
- Modify: `apps/web/src/features/ocr/lib/select-target-molecule.ts`
- Modify: `apps/web/src/features/ocr/hooks/useImageOcr.ts`
- Modify: `apps/web/tests/ocr-molecule-writeback.test.ts`

- [ ] Replace “last molecule wins” with a safer rule: explicit target first, unique molecule second, otherwise create a new block.
- [ ] Keep block id generation deterministic for new OCR insertions.
- [ ] Re-run targeted OCR tests until green.

### Task 10: Prevent OCR/Ketcher From Overwriting Newer Source Edits

**Files:**
- Modify: `apps/web/src/app/page.tsx`
- Modify: `apps/web/src/features/ocr/hooks/useImageOcr.ts`

- [ ] Convert source updates to functional state updates based on the latest source value.
- [ ] Ensure save/apply operations derive writeback from the latest state, not stale closures.
- [ ] Re-run targeted web tests until green.

### Task 11: Preserve Structure Draft Fidelity And Improve Cache Isolation

**Files:**
- Modify: `apps/web/src/features/structure-editor/lib/structure-save.ts`
- Modify: `apps/web/src/server/chem/dto.ts`
- Modify: `apps/web/src/server/chem/structure-store.ts`
- Modify: `apps/web/src/app/api/chem/structure/route.ts`
- Modify: `apps/web/src/app/api/chem/structure/save/route.ts`
- Modify: `apps/web/src/features/structure-editor/lib/load-structure-draft.ts`
- Modify: `apps/web/tests/structure-save.test.ts`
- Modify: `apps/web/tests/structure-store.test.ts`
- Modify: `apps/web/tests/load-structure-draft.test.ts`

- [ ] Keep `molfile` in save requests when present.
- [ ] Extend cache identity with a session-level discriminator passed through the route layer.
- [ ] Make local draft loading and persisted draft resolution match the new contract.
- [ ] Re-run targeted web tests until green.

## Chunk 4: Documentation Alignment

### Task 12: Fix V0.1/V0.2 Narrative Drift

**Files:**
- Modify: `docs/chemd-v0.1-spec.zh-CN.md`
- Modify: `docs/chemd-v0.1-architecture.zh-CN.md`
- Modify: `docs/chemd-v0.1-功能计划与实现进度.md`
- Modify: `docs/chemd-v0.2-training-export-plan.md`

- [ ] Keep docs honest: v0.1 default UI is `Editor + Preview`, `Tree` is experimental, molecule path is implemented shell-first, reaction Web/Ketcher path remains a target, and `training-export` belongs to v0.2.
- [ ] Remove residual v0.1 naming inside the v0.2 training-export plan.

## Chunk 5: Verification

### Task 13: Targeted Regression Verification

**Files:**
- Test only

- [ ] Run: `pnpm --filter @chemd/web test -- page.test.tsx ocr-molecule-writeback.test.ts load-structure-draft.test.ts structure-save.test.ts structure-store.test.ts chem-ocr-route.test.ts`
- [ ] Run: `pnpm --filter @chemd/parser test -- parser.test.ts`
- [ ] Run: `pnpm --filter @chemd/compiler test -- compiler.test.ts`
- [ ] Run: `pnpm --filter @chemd/renderer-html test -- renderer-html.test.ts`
- [ ] Run: `pnpm --filter @chemd/exporter-training test`
- [ ] Run: `python -m unittest discover -s services/chem-service/tests -p 'test_*.py'`

### Task 14: Full Workspace Confidence Checks

**Files:**
- Test only

- [ ] Run: `pnpm test`
- [ ] Run: `pnpm typecheck`
- [ ] If any command fails, fix or report the exact residual blockers before claiming completion.
