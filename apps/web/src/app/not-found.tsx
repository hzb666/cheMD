const NotFound = () => (
  <main className="flex min-h-screen items-center justify-center px-6 text-center">
    <div>
      <p className="text-sm uppercase tracking-[0.24em] text-[var(--muted)]">chemd</p>
      <h1 className="mt-4 text-3xl font-semibold text-[var(--ink)]">Page not found</h1>
    </div>
  </main>
);

export default NotFound;
