# Reaction Map Data Layer

Pure TypeScript data package for reaction-map nodes, edges, clusters, fallback layout, and TMAP worker contracts. It may read exporter-training graph index shapes but must not modify exporter-training.
