# Phase 5: State trace and semantic diff

Implement the next large code batch for state stack, runtime trace, and semantic diff behavior. Use code and tests as source of truth only. Do not read or change docs/spec files.

Acceptance: targeted package tests/typechecks pass, compiler matrix remains green if affected, Trellis validation and diff check pass.
